# LLM 파인튜닝 + RAG + Agent 실습 통합 패키지
# pip install -r requirements.txt
# (또는 권장: bash setup.sh — CUDA/Ollama/VS Code 포함 자동 설치)

# ─── Core ML ───
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.28.0
datasets>=2.18.0

# ─── Fine-tuning (Part 2, 3) ───
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0
deepspeed>=0.14.0
unsloth

# ─── LLM Serving (Part 3) ───
vllm>=0.4.0          # GPU 전용
llama-cpp-python     # GGUF 양자화 (Session 23)

# ─── 양자화 (Session 22, 25) — 환경 의존도 높음, 실패 가능 ───
auto-gptq            # GPTQ 양자화
autoawq              # AWQ 양자화

# ─── LangChain & RAG (Part 4) ───
langchain>=0.3.0,<1.0.0
langchain-openai>=0.1.0
langchain-community>=0.0.20,<1.0.0
langchain-ollama>=0.1.0
langchain-chroma>=0.1.0
langchain-experimental>=0.3.0  # LLMGraphTransformer (Session 30)
chromadb>=0.4.0
sentence-transformers>=2.6.0
rank_bm25>=0.2.2
faiss-cpu>=1.7.4
pypdf>=4.0.0                   # PDF RAG (Session 28)

# ─── Graph / Knowledge Graph (Part 4 — Session 30, 31) ───
networkx>=3.0
neo4j>=5.0.0                   # Neo4j 그래프 DB
psycopg2-binary>=2.9.0         # Apache AGE (PostgreSQL)
rdflib>=7.0.0                  # RDF/Turtle/SPARQL

# ─── Agent / MCP / A2A (Part 5 — Session 36, 37, 38, 40) ───
mcp[cli]>=1.2.0                # Model Context Protocol
langgraph>=0.2.0               # 멀티 에이전트 워크플로
httpx>=0.27.0                  # A2A HTTP 클라이언트

# ─── LLM API ───
openai>=1.12.0
anthropic>=0.40.0              # Claude API (Session 34)

# ─── Tokenization ───
tiktoken>=0.6.0

# ─── Data Processing ───
pandas>=2.0.0
numpy>=1.24.0
gensim>=4.3.0                  # Word2Vec (Session 2)

# ─── Evaluation (Part 4 — Session 32) ───
ragas>=0.1.0
nltk>=3.8.0
rouge-score>=0.1.2
bert-score>=0.3.13
evaluate>=0.4.0
scikit-learn>=1.3.0

# ─── Visualization / UI ───
matplotlib>=3.7.0
streamlit>=1.32.0

# ─── API 서버 ───
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
requests>=2.31.0

# ─── Utilities ───
python-dotenv>=1.0.0
tqdm>=4.66.0
huggingface-hub>=0.22.0
ipykernel
